using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Producing : MonoBehaviour
{
    public int Level = 1;
    public float GenerationSpeed = 60; //pcs/min 
    public float UpgradeCost = 60;
    public float Income = 60;//value/min
    public float CollectedAmount = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
